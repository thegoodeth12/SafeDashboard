import React from "react";
import ReactDOM from "react-dom/client";
import SafeDashboard from "./SafeDashboard";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<SafeDashboard />);
